# Firebase Configuration (already configured in firebase.ts)
# These are for reference only - Firebase config is hardcoded in the app

# API Configuration (optional)
EXPO_PUBLIC_API_BASE_URL=https://farm-help-383f1.firebaseio.com/
